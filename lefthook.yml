pre-commit:
  parallel: false
  commands:
    format:
      run: pnpm format:check
    lint:
      run: pnpm lint
    typecheck:
      run: pnpm typecheck

pre-push:
  parallel: false
  commands:
    unit:
      run: pnpm test:coverage
